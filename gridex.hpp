using namespace std;
void gridex(vector<vector<string>> &board, vector<vector<int>> &boardex, int x, int y, Avatar *av);
void boardex_to_board(vector<vector<int>> &, vector<vector<string>> &);
int available(vector<vector<int>> &, int, int y);
